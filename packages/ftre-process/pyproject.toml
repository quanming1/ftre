[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ftre-process"
version = "0.1.0"
description = "Cross-platform subprocess service for ftre"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
